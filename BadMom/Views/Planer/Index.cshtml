@using BadMom.Models.Planer
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Planer/_PlanerLayout.cshtml";
}




<div class="container">

    <div class="main-calendar">
        <div id="calendar"></div>
    </div>

</div>


<div class="modal fade" tabindex="20" role="dialog" id="showEventModal"  style="z-index: 1052;">
    <div class="modal-dialog modal-dialog-centered" role="document">
        <div class="modal-content" id="showEventDiv">

        </div>
    </div>
</div>

<div class="modal fade" tabindex="20" role="dialog" id="addEventModal">
    <div class="modal-dialog modal-dialog-centered" role="document">
        <div class="modal-content add-event-mod" id="addEventDiv">

        </div>
    </div>
</div>



<div class="modal fade show" tabindex="10" role="dialog" id="RemindEvent" >
    <div class="modal-dialog modal-dialog-centered" role="document">
        <div class="modal-content" id="RemindEventDiv">
            <div class="modal-header">
                <h5 class="modal-title">Напоминание</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                @if ((ViewBag.Remind as List<EventVM>).Count > 0)
                {
                    foreach (var item in (ViewBag.Remind as List<EventVM>))
                    {
                        <div class="d-flex justify-content-between">
                            <div class="">
                                @item.Title
                            </div>
                            <div class="">
                                @item.DateStart.ToShortDateString() | @item.DateStart.ToShortTimeString()
                            </div>
                            <div class="">
                                <a href="javascript:ShowEventDetailsModal(@item.Id);">Подробнее</a>
                            </div>
                        </div>
                    }
                }
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-dismiss="modal">Закрыть</button>
            </div>
        </div>
    </div>
</div>

@section scripts{
    @*<script>
            var getEventsUrl = '@Url.Action("GetUserEvents", "Planer")';
            var showDate = '@ViewBag.ShowDate';
            console.log(showDate);
        </script>*@
    @if ((ViewBag.Remind as List<EventVM>).Count > 0)
    {
        <script>
            var Remind = true;
        </script>
    }



}
